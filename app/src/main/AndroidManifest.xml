<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.ac.ict.cana">

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.PermissionActivity_"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity_"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".activities.UserActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Count-->
        <activity android:name=".modules.count.CountMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".modules.count.CountingActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".modules.count.NextActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Stride-->
        <activity android:name=".modules.stride.GoActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".modules.stride.StrideMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Stand-->
        <activity android:name=".modules.stand.StandMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".modules.stand.StandTestingActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Face-->
        <activity android:name=".modules.face.FaceMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>
        <activity android:name=".modules.face.VideoCaptureActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Tapper-->
        <activity android:name=".modules.tapper.TapperMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <!-- Module: Sound-->
        <activity android:name=".modules.sound.SoundMainActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

    </application>

</manifest>